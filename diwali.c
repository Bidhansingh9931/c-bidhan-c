#include<stdio.h>
#include<conio.h>
#include<dos.h>
void main()
{
    int i;
    // textcolor(RED);
    // textbackground(WHITE);
    for(i=1;i<=120;i++)
    {
        printf("happy diwali all of you");
         delay(200);
    }
    getch();
}